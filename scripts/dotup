#!/usr/bin/env bash
set -euo pipefail

repo="$HOME/.dotfiles"
post="$repo/scripts/dotpost"

echo "[dotup] pulling latest…"
git -C "$repo" pull --ff-only        # abort if merge needed

if [[ -x "$post" ]]; then
  "$post"
fi

echo "[dotup] done."
